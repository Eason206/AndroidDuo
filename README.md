# Fold POC

Phase 1–2 Android proof of concept. The slider drives a normalized `foldProgress` value from 0.0 (folded) to 1.0 (unfolded). `FoldRenderView` is intentionally isolated so the placeholder planes can later be replaced by AGSL/runtime-shader geometry and a hinge-angle source.
